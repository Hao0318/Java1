package OOP.Method;

public class Vowls {

    public static int vowlsNum(String str) {

        int len = str.length();

        for (int i = 0; i < len; i++){
            String ch = str.substring(i, i+1);
            char s = ch.charAt(0);
            if (s == 'a'|| s == 'e' || s == 'i' || s = 'i' || s == 'o'){

            }

        }



    }
}
